import React, { memo } from 'react';

/**
 * Componente para manejar la selección de rangos de celdas
 * Este componente es principalmente lógico, el renderizado visual
 * se maneja en CalculatorCell mediante las props isSelected e isInRange
 */
const CalculatorRangeSelector = memo(() => {
    // Este componente es principalmente para lógica futura
    // La selección visual se maneja en CalculatorCell
    return null;
});

CalculatorRangeSelector.displayName = 'CalculatorRangeSelector';

export default CalculatorRangeSelector;

